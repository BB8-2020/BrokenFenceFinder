# Broken Fence Finder

[![Build](https://github.com/BB8-2020/BrokenFenceFinder/actions/workflows/python-build.yml/badge.svg)](https://github.com/BB8-2020/BrokenFenceFinder/actions/workflows/python-build.yml)

# Install
```bash
pip install .
```

# For developers
Developers on Visual Studio might want to rename/copy `.vscode/settings.json.example` to `.vscode/settings.json`.  
This let's Visual Studio recognize the linter so you'll get corrected in your IDE instead of finding out when trying to merge.